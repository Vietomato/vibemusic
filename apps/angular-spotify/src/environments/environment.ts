import { AppConfig } from '@angular-spotify/web/shared/app-config';

export const environment: AppConfig = {
  production: false,
  // ĐÂY MỚI LÀ ĐỊA CHỈ THẬT CỦA SPOTIFY NÈ BÀ:
  baseURL: 'https://api.spotify.com/v1' 
};